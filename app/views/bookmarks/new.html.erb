<h1>Add Movie</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, collection: Movie.all, prompt: "Choose a Movie", label: "Movie" %>
  <%= f.input :comment, label: "Comment" %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to List", list_path(@list) %>
